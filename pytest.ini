[pytest]
testpaths =
    pyAOBS/tests
python_files = test_*.py
markers =
    unit: fast unit tests that do not rely on external binaries or large datasets
    integration: tests that rely on external tools, files, or long-running workflows
